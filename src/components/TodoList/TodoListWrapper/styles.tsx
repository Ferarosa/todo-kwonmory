import styled from '@emotion/styled';

const Wrapper = styled.div`
	display: flex;
	width: 20rem;
	height: 30rem;
	position: relative;
	padding-left: 20rem;
	box-shadow: 1px 1px 25px 10px rgba(0, 0, 0, 0.2);
	border-radius: 0.4rem;
	background-color: transparent;
	user-select: none;
`;

export { Wrapper };
